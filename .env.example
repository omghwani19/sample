# Optional. If omitted or missing, the app uses the built-in guide config.
GAT_GUIDE_PATH=C:\Users\hwani\Downloads\GAT_SEARCH_GUIDE_v2026.txt

# Optional. Default: 4173
PORT=4173

# Optional. Default: google-news-rss. Use google-cse only if you want Custom Search API mode.
SEARCH_PROVIDER=google-news-rss

# Optional. Only required when SEARCH_PROVIDER=google-cse.
GOOGLE_API_KEY=
GOOGLE_CSE_ID=
